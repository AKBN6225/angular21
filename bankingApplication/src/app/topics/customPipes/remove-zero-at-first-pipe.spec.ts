import { RemoveZeroAtFirstPipe } from './remove-zero-at-first-pipe';

describe('RemoveZeroAtFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveZeroAtFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
